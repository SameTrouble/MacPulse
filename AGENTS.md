# AGENTS.md

## Agent skills

### Issue tracker

Issues tracked in GitHub Issues (`SameTrouble/MacPulse`) via `gh` CLI. See `docs/agents/issue-tracker.md`.

### Triage labels

默认五个 triage 标签(needs-triage, needs-info, ready-for-agent, ready-for-human, wontfix). See `docs/agents/triage-labels.md`.

### Domain docs

Single-context: 根目录 `CONTEXT.md` + `docs/adr/`. See `docs/agents/domain.md`.
